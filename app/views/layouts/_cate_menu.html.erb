<div class="navbar-default center">
      <ul class="nav nav-pills">
        <% Category.where(parent_id: '1').each do |category| %>
          <li id="<%= category.id%>">
          <%= link_to category.name, getcate_path(select_cate: category.id) %>
          </li>
        <% end %>
      </ul>
    </div>
    <!-- <div class="navbar-default center">
      <ul class="nav nav-pills">
        <% Category.where(parent_id: '1').each do |category| %>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= category.name %></a>
            <ul class="dropdown-menu">
            <li><%= link_to 'Tất cả', getcate_path(select_cate: category.id) %></li>
            <%category.children.each do |subcate| %>
              <li><%= link_to subcate.name, getcate_path(select_cate: subcate.id) %></li>
            <% end -%>
            </ul>
          </li>
        <% end %>
      </ul>
</div> -->